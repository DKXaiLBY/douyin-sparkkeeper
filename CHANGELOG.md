# 更新日志

本项目所有值得记录的变更都会写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 文档

- Docker 部署完成真机验证（2026-09-05，Docker Desktop 29.7.2 / WSL2 后端）：**含 Chromium 的完整镜像（约 1.97GB）首次构建成功**，容器启动、健康检查、前端静态托管、`/data` 持久化、重启自恢复、容器内 Chromium（151.0.7922.34）无头启动均实测通过；douyin 真实模式的扫码登录与真实发送仍需使用者自行登录验证
- README 补充国内网络加速构建方法：apt 可换清华镜像源；浏览器包（Chrome for Testing，约 313MB）可从 npmmirror 镜像下载后经本地静态服务器 + `PLAYWRIGHT_DOWNLOAD_HOST` 注入构建（官方 CDN 国内限速严重且按 IP 限速）

## [1.0.0] - 2026-09-03

首个可用版本。核心思路：**不只是一个"跑完就没了"的脚本，而是一个看得见、管得住的看板**。

### 新增

**可视化看板**
- **今日待续**：顶部主角区，一眼看到还差谁没发、剩余多少小时，支持一键全发
- **今日概览**：守护好友数 / 今日已发 / 最长连续天数
- **火花好友**：每个好友的连续天数与等级，支持从抖音会话一键拉取批量添加
- **续火热力图**：近 30 天 3×10 网格，点开是**按月翻页的月历**（‹ › 切换月份，每月天数自动正确）
- **顶部常驻**：30 天发送成功率 + 异常通知铃铛（掉线 / 验证码 / 发送失败主动提醒）

**自动化与安全**
- 扫码登录，登录态经 **AES-256-GCM** 加密存本地，明文不落盘、零遥测
- **Dry Run 演练**：真的开一遍浏览器验证"能不能找到好友/输入框/发送按钮"，但绝不点发送
- **安全模式**（默认开启）：随机延迟 + 错峰窗口（默认 19–23 点）+ 每日上限 + 连续失败熔断
- **窗口内每日随机时刻**发送（默认）——不固定时间，行为更像真人，降低被模式识别的概率
- 定时时刻与"今日已触发"标记会持久化，服务重启不会重复补发

**首次使用体验**
- **风险与免责声明**：首次打开必须先确认，明确告知违约与封号风险由使用者自担
- **分步引导**：扫码登录 → 添加好友 → 完成（含日常使用与配置指引），支持上一步 / 跳过
- **设置页 Tab 化**：引擎 / 账号 / 安全 / 通知 / 文案，页面不再臃肿
- 设置页底部常驻「重新查看引导」入口，随时可以再看一遍

### 说明

- Docker 部署路径已做静态审查并修复已知问题（`.dockerignore` 防止含凭证的 `server/data/` 入镜像、容器内补齐 Playwright/Chromium），
  但**开发环境无 Docker，未做真机运行验证**——推荐用本机运行方式，跑通了欢迎提 PR。
- 使用真实抖音模式前需手动安装 Playwright：`npm i playwright && npx playwright install chromium`
  （项目对它是按需加载，只玩 mock 演示模式则不需要）。

### 安全

- 首次提交前已清理全部运行时数据（数据库、加密凭证、配置、诊断截图），仓库中不含任何个人信息。
- `.gitignore` 已排除 `data/`、`credentials/`、`data-*/`、`*.db`、`*.enc.json`、`storage_state.json` 等，
  后续使用产生的数据也不会被误提交。
