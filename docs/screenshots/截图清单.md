# 截图清单（对齐 v1.0.0 当前界面）

README 顶部引用了 4 张图，请把截图放到本目录，文件名保持一致：

| 文件名 | 截什么 | 怎么截 |
| :--- | :--- | :--- |
| `dashboard.png` | **仪表盘全景（最重要）** | 首页完整截一屏：顶部「今日待续」大数字 + 概览 + 好友区 + 底部热力图。**这张图决定别人要不要点 Star** |
| `friends.png` | 火花好友区 | 截「火花好友」那一栏，2–3 个不同昵称的好友即可 |
| `heatmap.png` | 续火热力图 + 月历弹窗 | 截 3×10 热力图；再点开「查看完整日历」截一张月历弹窗（可拼一起或选月历那张） |
| `settings.png` | 设置页 | 截设置页（能同时看到 tab 栏 + 一个面板，如「安全」tab） |

## 截图前的准备（10 分钟，让图好看）

数据已清空，直接截是空白页。按下面造一点演示数据（**纯假数据，无隐私风险**）：

1. 双击 `启动.bat`（后端默认 mock 演练模式，不需要真的抖音账号）
2. 浏览器打开 http://localhost:5173 → 同意协议 → 关掉引导
3. 回仪表盘 → 「火花好友」→「＋ 添加」→ 点 **「🔄 从抖音拉取好友」** → 会列出演示好友（小雨同学 / 阿凯 / 测试号）→ 勾选 → 批量添加
4. 顶部「今日待续」→ 点 **「全部发送」**（mock 模式会真的走一遍发送流程并记录，热力图会亮起格子）
5. 数据就有了：成功率 100%、热力图有橙色格子、今日待续显示"已全部续上"

## 截图技巧

- 浏览器 **Ctrl+0** 复位到 100% 缩放（别放大，字会糊），窗口拉宽到 1280–1600px
- **Win + Shift + S** 区域截图，PNG 格式
- 仪表盘全景建议截到完整底部（热力图露出几行 + 图例）
- 液态玻璃的柔光要能看出来——确保背景光球在卡片后面，别在纯色窗口下截

## 隐私红线（图会公开到 GitHub）

- ✅ 用上面 mock 演示数据截（假昵称、无真实信息）
- ❌ 不要用真实抖音账号登录后截（会露出真实好友昵称/会话）
- ❌ 不要截到凭证保险库已解锁状态的界面（含口令输入框也别截）
